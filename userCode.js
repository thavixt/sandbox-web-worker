$.mySandboxFunction('DEMO - get increasing number digits of PI');
// setTimeout(() => $.mySandboxFunction('I was delayed in user code by 10 sec - BUT might not be seen since the sandbox might have been terminated by then (bug?)'), 10_000);
